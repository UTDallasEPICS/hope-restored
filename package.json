{
  "name": "hope-restored",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm run -r build && rm -rf ./.output ./admin/.output/public/webcomponent && cp -r webcomponent/dist ./admin/.output/public/webcomponent && cp -r ./admin/.output ./.output",
    "generate": "prisma generate",
    "db:migrate:dev": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:seed": "prisma db seed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@prisma/adapter-better-sqlite3": "^7.0.0",
    "@prisma/client": "^7.0.0",
    "better-sqlite3": "^12.0.0",
    "dotenv": "^17.4.2",
    "prisma": "^7.0.0",
    "tsx": "^4.19.2"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
